%p#notice= notice
.container
  .row
  .col-med-2
  .col-med-8
  #bookstitle Books you have added
  %p
  You have #{@user.books.count} books in your library
  %table#mylibrary.display/
  %thead
    %tr
      %th Cover
      %th Title
      %th Description
      %th Author
      %th{ colspan: 2 }
  %tbody
    - @books.each do |book|
      %tr
        %td
          %img{:align => "center", :src => book.image_url, :vspace => "8", :width => 60, :height => 80}/
        %td= link_to book.title, book
        %td= truncate book.description, :length=>200
        %td= book.author
        %td= link_to 'Edit', edit_book_path(book)
        %td= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' }
  .col-med.2
